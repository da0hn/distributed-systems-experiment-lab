/**
 * Esqueleto da interface. Nenhuma tela existe ainda: o Experiment Designer
 * depende da posicao 8 da fila de decisoes, e o formato do veredito da
 * posicao 9.
 *
 * O framework de componentes e de estilo continua em aberto — e a linha
 * `D-UI-03`, que nao foi decidida. Nada aqui deve antecipa-la.
 */
export function App() {
  return (
    <main>
      <h1>Distributed Consistency Lab</h1>
      <p>
        O esqueleto esta de pe. Nenhuma capacidade foi implementada, e nenhuma
        tela foi desenhada.
      </p>
    </main>
  );
}
